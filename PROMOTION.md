# EvoRule 本地体验包 · 推广文案

## 一句话介绍

EvoRule 是一个把合规策略变成可执行、可审计、可回溯规则的确定性引擎。这个体验包让你在 7 分钟内亲手跑完「规则加载 → 门禁阻断 → BLAKE3 审计查证」全链路——真实引擎，不是 Mock。

---

## ⚡ 现在就能玩：打开网页，0 秒上手

**[👉 立即体验在线 Demo →](https://evorule.github.io/evorule-console-cloud/)**

**零安装、零配置、零后端**——evorule 引擎被编译成 WebAssembly，直接在你的浏览器里跑。不用下载、不用装 Rust / Node / Docker、不用起任何服务：

- 打开链接即用，**16 条业务规则全部在线**（财务 6 + 医疗 6 + 等保 4），门禁阻断当场可见
- **BLAKE3 审计链在浏览器里当场验证通过**，篡改即被发现
- 首访一次后 **Service Worker 缓存，离线也能用**
- 7 步交互式引导教学，跟着走一遍就懂

> 国内访问 GitHub Pages 偶尔较慢：可稍后重试，或直接下载下方本地体验包（完全离线，不受网络影响）。

想继续深入（热重载 / Bundle 管理 / 持久化）？再往下看本本地体验包。

---

## 30 秒电梯演讲

> 你写了 100 条业务规则，怎么证明它们每次都按预期执行？怎么在出问题时 30 秒内定位根因？怎么向审计师证明决策没被篡改？

EvoRule 的答案：
- **确定性**：同入同出，同命令执行两次审计哈希一致
- **全审计**：每次决策 append-only + BLAKE3 哈希链，篡改即被发现
- **可回放**：时间旅行回溯任意版本，diff 对比规则修改前后
- **合规门禁**：违规操作被阻断 + 阻断事件本身被审计记录

---

## 三个真实场景（体验包里就能跑）

### 场景一：财务——5000 元差旅报销被拦

提交 `{expense:{type:"travel", amount:5000}}`，引擎返回：
```
decision: blocked
reason: 差旅报销超限额3000元
```
依据：财会〔2008〕7号《企业内部控制基本规范》授权批准控制。改限额 3000→5000，热重载后再提交，立即放行。

### 场景二：医疗——门诊开万古霉素被拦

提交 `{prescription:{antibiotic_tier:"special"}, visit:{type:"outpatient"}}`，引擎返回：
```
decision: blocked
reason: 特殊使用级抗菌药物不得在门诊使用（卫生部令84号第二十七条）
```
依据：《抗菌药物临床应用管理办法》卫生部令第84号。

### 场景三：等保——身份证号明文存储被拦

提交 `{operation:{fields:{id_card:"..."}}, encryption:"none"}`，引擎返回：
```
decision: blocked
reason: 等保2.0三级 §8.1.4.8.b：敏感数据未加密存储，已阻断并留痕
```
依据：GB/T 22239-2019 三级安全计算环境数据保密性要求。

---

## 查证：不只是"拦了"，而是"可证明拦了"

每次阻断后：
1. `GET /audit/verify` → `{verified:true}`，BLAKE3 链完整
2. `GET /audit/causal/{fact_id}` → 从指令到决策的因果链
3. `GET /rewind?version=1` → 回看阻断前的状态
4. `GET /diff?a=1&b=2` → 规则修改前后的 payload 差异

---

## 包里有什么

- **真实引擎**：evorule-server + evorule-rule-serve 双二进制（Rust 编译，非解释器 Mock）
- **16 条规则**：财务 6 + 医疗 6 + 等保横切 4，每条标注法规条款号
- **20 条预置数据**：6 报销单 + 4 发票 + 6 患者档案 + 4 处方，全脱敏
- **6 个规则 bundle**：含等保横切独立包，可导入导出
- **前端控制台**：SvelteKit 构建，浏览器即开即用
- **一键启动**：Windows 双击 .bat，Linux bash .sh

---

## 与在线 Demo 的区别

> 在线 Demo 现在跑的是**真实 evorule 引擎（编译为 WASM）**，不再是 Mock——同样的规则、同样的 BLAKE3 哈希链，只是跑在浏览器里。

| | 在线 Demo（WASM） | 本地体验包 |
|---|---|---|
| 引擎 | 真实 evorule 引擎编译为 WebAssembly，浏览器内运行 | 真实 Rust 引擎（`evorule-server` 二进制） |
| 审计链 | BLAKE3 哈希链，浏览器内当场验证 | BLAKE3 哈希链 + 导出 / 离线核验 |
| 规则 | 16 条合规规则全部在线可跑 | 16 条合规规则可编辑 + 热重载 |
| 离线 | ✅ 首访后 Service Worker 缓存，离线可用 | ✅ 完全离线 |
| 数据 | 内存态，刷新后会话重置 | WAL 持久化 |
| 启动 | 打开链接即用 | 下载后双击 `start.bat` |
| 热重载 / Bundle | ❌ 在线不支持 | ✅ 改规则热重载 + 6 个 bundle 导入导出 |

---

## 适用人群

- **合规/审计人员**：亲手验证规则阻断 + 审计留痕的完整链路
- **开发者**：理解声明式规则引擎的执行模型和审计机制
- **架构师**：评估 evorule 作为 AI Agent 合规中间层的可行性
- **技术布道者**：有真实可跑的 demo 做技术分享

---

## 获取与启动

1. 解压 `evorule-experience-pack.zip`
2. Windows 双击 `start.bat`，Linux 执行 `bash start.sh`
3. 浏览器自动打开 http://localhost:18080
4. 按 `QUICKSTART.md` 7 步走完

全程无需安装 Rust、Node、Docker，无需联网。

---

## 技术诚实

- 引擎无循环/聚合原语：拆单检测的分组合计由上游预计算，规则只编码处置策略
- TCB 无 Float：金额用整数，体温用十分度整数
- 单反应器演示：避免高频新建会话
- 演示数据全虚构脱敏，不构成正式合规背书或临床依据
- 在线 WASM 版与本地引擎同源同规则，但不支持规则热重载 / Bundle 管理 / 持久化（刷新重置）；`role13_demo` 的 `io_request` 场景不收敛，需本地体验
